Objective:

The objective of this task is to connect Python with SQLite database, run SQL queries, calculate total quantity and revenue, and display the result using print statement and bar chart.

Tools Used:

1.Python 2.SQLite (sqlite3) 3.Pandas 4.Matplotlib 5.Python IDLE

Process:

Connected Python to SQLite database using sqlite3. Created a sales table with product, quantity, and price columns. Inserted sample sales data into the table. Executed SQL query to calculate total quantity and revenue using GROUP BY. Loaded the result into pandas DataFrame.

Printed the output.

 Created a bar chart using matplotlib.
Output:

1. Displayed sales summary table.
2. Generated bar chart showing revenue by product.
Files Included:

1.task7_sales_summary.py (Python code) 2.sales_data.db (Database file) 3.sales_chart.png (Bar chart image)

Conclusion:

This task helped me understand how to use SQL inside Python, analyze data using pandas, and visualize results using matplotlib.
